# Bundled datasets

These compact, checksummed, read-only projections are displayed immediately after a clean installation. `fgfr2_30_species` is the validated thesis dataset; the PTPN11 3-species run is an exploratory generic demonstration. Mutable new runs are stored separately in the user's configured application data directory.
