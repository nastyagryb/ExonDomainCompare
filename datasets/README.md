# Bundled datasets

These compact, checksummed, read-only projections are displayed immediately after a clean installation. `fgfr2_30_species` is the validated thesis dataset; the BCL2L1 human/mouse run is an exploratory generic demonstration. Mutable new runs are stored separately in the user's configured application data directory.
